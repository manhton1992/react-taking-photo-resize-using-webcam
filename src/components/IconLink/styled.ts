import styled from 'styled-components'

const StyledInconLink = styled.a`
    margin-right: 16px;
    line-height: 24px;

    & > img {
        margin-right: 8px;
    }
`

export default {
    StyledInconLink,
}
